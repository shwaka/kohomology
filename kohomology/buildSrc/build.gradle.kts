plugins {
    `kotlin-dsl`
}

// kotlinDslPluginOptions {
//     experimentalWarning.set(false) // This has no effect in Gradle 7.4.2 (and will be removed in 8.0)
// }

repositories {
    mavenCentral()
}
